name: AD Mooncake reverse

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  mooncake-reverse:
    uses: ./.github/workflows/ad-backend.yaml
    with:
      backend: 'Mooncake reverse'
